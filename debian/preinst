#!/bin/sh

#https://wiki.debian.org/MaintainerScripts

set -e

case "$1" in
    install)
        #nothing to preinstall here.
        #python dependencies are installed by cleepos upgrade process
        ;; 

    *)
        echo "preinst called with unknown argument '$1'" >&2
        ;;
esac

#DEBHELPER#

exit 0

